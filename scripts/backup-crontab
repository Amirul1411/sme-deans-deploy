SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin

# Run backup every hour
0 * * * * /scripts/backup.sh >> /backups/cron.log 2>&1

# Run monitoring every hour
0 * * * * /scripts/monitor.sh >> /backups/cron.log 2>&1 